state => counter, setCounter 
context => useContext 
------------------------------
redux => redux-toolkit
------------------------------
Redux Toolkit (RTK) is the official, recommended way to write Redux logic.
It simplifies working with Redux by removing boilerplate and providing built-in tools for:

State management
Async API calls
Immutable updates
------------------------------
dispatch -> action -> reduce
------------------------------
npm install @reduxjs/toolkit react-redux
--------------------------------

useSelector => reads data from the Redux store.
useDispatch => Used to send actions.
------------------------------------
npm install axios